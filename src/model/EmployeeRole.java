package model;

/**
 * EmployeeRole enum: Represents the different job roles an employee can have.
 * Enums (enumerations) are a special type of class that represents a fixed set of constants.
 */
public enum EmployeeRole {
    DEVELOPER,
    QA_ENGINEER,
    PROJECT_MANAGER,
    HR_SPECIALIST,
    ADMINISTRATOR
}